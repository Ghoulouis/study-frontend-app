import { Box, Button, Typography } from "@mui/material";

export default function Dashboard() {
    return (
        <Box>
            <Typography mb={1}>Dashboard</Typography>
        </Box>
    );
}
